<%- include('./../partials/head.ejs', {title: "Login"}); %>

<% posts.forEach(post => { %>
    <div class="mt-10">
        <h2 class="font-bold"><%= post.title %></h2>
        <p class="mt-3 text-sm"><%= post.created_at %></p>
        <p class="mt-3"><%= post.content %></p>
    </div>
<% }); %>

<%- include('./../partials/footer'); %>